<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - 醫療暨工務設備管理系統</title>
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/menu/css")
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
    <script src="~/Scripts/jquery-migrate-3.0.0.js"></script>
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">

            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <img style="width:60%; height:60%; padding:5px 15px 5px 0px" src="~/Images/logo.png" />

            </div>
            <div class="navbar-collapse collapse">
                @Html.ActionLink("醫療暨工務設備管理系統", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    @if (User.Identity.IsAuthenticated)
    {
        <div id="menu">
            <nav id="nav" class="ry">
                <ul id="main-menu">
                    <li>
                        @Html.ActionLink("首頁", "Index", "Home")
                    </li>
                    <li>
                        <a href="#">申請表單</a>
                        <ul class="submenu">
                            @Html.Action("GetFuncs", "Funcs", new { module = "申請表單" })
                        </ul>
                    </li>
                    <li>
                        <a href="#">醫療儀器</a>
                        <ul class="submenu">
                            @Html.Action("GetFuncs", "Funcs", new { module= "醫療儀器" })
                        </ul>
                    </li>
                    <li>
                        <a href="#">財務</a>
                        <ul class="submenu">
                            @Html.Action("GetFuncs", "Funcs", new { module = "財務" })
                        </ul>
                    </li>
                    <li>
                        <a href="#">統計報表</a>
                        <ul class="submenu">
                            @Html.Action("GetFuncs", "Funcs", new { module = "統計報表" })
                        </ul>
                    </li>
                    <li>
                        <a href="#">查詢</a>
                        <ul class="submenu">
                            @Html.Action("GetFuncs", "Funcs", new { module = "查詢" })
                        </ul>
                    </li>
                    <li>
                        <a href="#">管理</a>
                        <ul class="submenu">                        
                            @Html.Action("GetFuncs", "Funcs", new { module = "管理" })
                            @if (Roles.IsUserInRole("Admin"))
                            {
                                <li>@Html.ActionLink("功能權限設定", "RoleWithFuncs", "Funcs")</li>
                            }
                        </ul>
                    </li>
                </ul>
            </nav>
        </div>
    }
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - 國立陽明大學附設醫院　工務室</p>
            <p>若有相關問題請撥打工務室分機：10076，電話佔線時請耐心等候</p>
            <p>蘭陽院區 : 26058 宜蘭市校舍路169號</p>
            <p>新民院區 : 26042 宜蘭市新民路152號</p>
        </footer>
    </div>

    @Scripts.Render("~/Scripts/menu.js")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
